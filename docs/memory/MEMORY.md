# Memory index

Per-project memories for **steel-sim** (steel simulator), extracted from the BigSim
program's shared memory on 2026-06-10 when the monorepo was split into standalone repos.
Cross-cutting *program* memories (bigsim-program, parallel-dev, test-execution-policy,
the notebook-discipline notes, the github-repo pointer) stayed in the BigSim archive store;
`[[links]]` to those will dangle here by design.

- [Steel-making front-end + game plan](steel-making-frontend-plan.md) — **NEW DIRECTION 2026-06-12 (plan-only)**: `docs/plans/steel-making.md` opens the FRONT half (ore→billet steel-*making*) + a gamified full-chain spinoff with failure-propagation, companion to the heat-treatment back half. Key calls: triad-gates real-vs-game; front-end wall = transport kinetics (build equilibrium endpoints + lumped rates only); `Heat` state in a thin ORCHESTRATOR not the frozen engines; in-repo `game/` now, repo-split DEFERRED; hero demo = alloy→hardenability→Jominy→quench-crack (all back-end-built). F1 Ellingham = first slice
- [F4 casting built](f4-casting-built.md) — **front-end F4 (casting, Slice 1) BUILT ✓ 2026-06-12**: the link that **closes the chain front-to-back** (`casting.py` + demo + figure + 22 tests; 542→**564 green**; no solver/engine/ADR). **REFRAME (plan premises wrong, verify yourself):** repo's "Scheil" is *additivity* (kinetics) NOT *microsegregation*; no solidification thermo in repo at all → F4 is **new physics, not the planned "thin reuse"**. Advisor steer: proof rides on **microseg → composition handoff through [[heat-state-spine-built]] spine, NOT the latent-heat solve** → Slice 1 needs **no solver**. Built = Scheil `C_s=k·C₀·(1−f_s)^(k−1)` + **centerline-enriched Heat** (real `"cast"` origin) + Chvorinov `t=B·M²`. **Proof:** 4140 casting heat-treats **non-uniformly** — bulk 64%M/533HV soft-core vs centerline 91%M/626HV = **+93HV hard band** (§6 uneven-hardenability). Teeth = **conservation mass-balance** (CAUGHT a real quadrature bug at the f_s→1 singularity) + **severity ordering** (S/C/P worst — un-tuned). **k provenance = 2 honest tiers** ([[di-crosscheck-source]], advisor caught overclaim): Won-Thomas C/Si/Mn/P/S **PDF-verified** (teeth rest here); Cr/Ni/Mo **ISIJ in-situ γ-mode, representative, δ not pinned** (WebFetch-verified after a paraphrase slip). Carbon EXCLUDED (Scheil over-predicts interstitial). User: "no frozen engines" → dropped framing in F4 docs; **repo-wide de-frost = separate approved pass, NOT done**. **Slice 2 DEFERRED:** latent-heat T-field map (NOT a trivial source term) + Niyama/hot-tear defects. **Next = F2/F3** or Slice 2. Amends [[heat-state-spine-built]]
- [Heat-state spine built](heat-state-spine-built.md) — **front-end SPINE (build-order item 2: `heat_state.py`) BUILT ✓ 2026-06-12**: the `Heat` immutable physical-state carrier (composes the back-end `Steel`; gas/inclusion/residual fields `None`="no engine produced it yet") + the thin orchestrator seam (unpacks Heat→Steel→**public `sweep.evaluate`**→repacks + appends a `ProcessStep` provenance trail). **Failure propagation** = `heat_treat` general path: under-dose 4140 Cr/Mo → same oil quench 96%/632 HV → **40%/416 HV soft-core flag** (back-end martensite crossing `MIN_MARTENSITE_SPEC`, a labelled **spec** not a fitted constant). **Advisor catch:** §18 residual is **grade-keyed/atlas-anchored** ({1080,4340}, ∩ back-end STEELS = only 1080) → off-spec→crack chain **DEFERRED**; `quench_crack_check` = fixed-4340 illustration (+386 MPa tension, consistent w/ [[residual-stress]]). **No physics/triad** — structural teeth (round-trip identity, immutability, deterministic propagation, atlas guard). Gallery drift-guard **forced a figure** (every demo must be catalogued w/ a banked figure) → bars+spec-line = propagation plotted (advisor reversed own "no figure" steer); new "Front-end spine" [[gallery-page]] section. 522→**542 green**, no engine touch/ADR. **Next (F4 casting) now BUILT → [[f4-casting-built]]** (and it turned out to be new physics, not the "frozen heat engine + existing Scheil" reuse imagined here). Amends [[f1-ellingham-built]]
- [F1 Ellingham built](f1-ellingham-built.md) — **front-end FIRST SLICE BUILT ✓ 2026-06-12**: `steel/reduction.py` (Ellingham ΔG°(T) per mole O₂ from NIST/CODATA per-species ΔHf/S°, **no fitted constant**), standalone (no engine/back-end touch, no ADR). Teeth = carbon/wüstite crossover **746 °C** (window 650–800, generous on purpose — wüstite non-stoichiometry slides it 710…746) + Fe₂O₃→Fe₃O₄→FeO→Fe stack (stepwise inter-oxide rxns) + Ca<Al<Si<Mn<Cr<Fe hierarchy + JANAF ΔfG°(CO,1000K)≈−200 anchor; by-construction = balance/ΔG(298)-identity/Hess. [[di-crosscheck-source]] applied (ΔHf/S° NIST-verified before pinning). Ceiling = straight-line ΔCp=0 (kinks omitted). pO₂ built (F2 bridge); demo+figure+gallery "Ironmaking" card+README, **notebook/app DEFERRED** (advisor). Suite 526→**546 green**. **Next (item 2 `heat_state.py`) now BUILT → [[heat-state-spine-built]].** Builds on [[steel-making-frontend-plan]]
- [Historical impurity pedagogy](historical-impurity-pedagogy.md) — **research fold 2026-06-12 (assess-only)** of "bad"/impure historical steel (P, S, slag inclusions) into `steel-making.md` §14 + a `steel-production.md` §11 back-end pointer. Reframe: archaeometallurgy data clusters on **toughness/embrittlement/inclusion-cracking** (not the weak Jominy/CCT/residual axes) → **corroborates** the verified/plausible split. Spine = **P is the signed-impurity FOIL to §5b grain refinement** (refine = 1 variable helps both; P = strength↑ lattice / toughness↓ GB-segregation, 2 mechanisms opposite). Strength **+237 MPa/at% P** (Thiele) pinned-able; **P→DBTT slope UNPINNED** (~40–70 °C/0.1% — real effect, not a coefficient; §5b ITT has no P term). Cleanest new triad clearers = P-strengthening + **Mn:S→MnS** (conservation-clean; Mushet/Bessemer history free); D inverse-hardness + E Scheil-ghost-lines **reuse** existing engines. Future-research benchmarks listed; wootz V/Mo = "good impurity" caveat. Companion to [[steel-making-frontend-plan]]
- [Bainite anchoring probe](bainite-anchoring-probe.md) — 6b follow-up (2026-06-10): austempering anchoring vs US Steel 1951 atlas (public domain) = per-steel PASS ~×1.3 (anchor at t50) / cross-composition FAIL ×14-35 wrong-direction (BC carbon coeff; per-steel scale table required); 6b's Cr/Mo mechanism teeth CONFIRMED by atlas (bainite ~4× vs pearlite ~10³×); **Phase 6d (austempering) BUILT 2026-06-10** — `austemper.py` per-steel import-derived scales (×41 gap = pinned negative) + holdout teeth (1080 t50 ×1.06/×0.96) + notebook/app §6, gate 319→339, pathint byte-identical; plan §13 = as-built record; **6c BUILT 2026-06-10 → [[di-crosscheck-source]], atlas-E-Q-panel candidate dropped (IT-atlas page collision)**
- [Gallery page](gallery-page.md) — `docs/index.html` = generated clickable front-door gallery (every demo's figure + run command + notebook/app links); single source = `CATALOG` in `steel/gallery.py`, regenerate `python -m steel.gallery`, `test_gallery.py` byte-compare drift-guard fails the build if stale → **rerun the generator after adding a demo/editing a blurb**; Pages-ready from `/docs` (not enabled); built 2026-06-12
- [Carburize diffusivity source](carburize-diffusivity-source.md) — cited Callister C-in-γ-Fe Arrhenius D(T) (constant, default erfc) **+ Tibbetts 1980 D(C)** (opt-in, BUILT 2026-06-11 — the diffusion engine was **unfrozen for native nonlinear `D(u)`**, re-sealed v1.1; ADR 0004 / plan §15)
- [Cross-composition validation (§20)](cross-composition-validation.md) — **bainite "per-steel only / 8620 wall" MEASURED on 8 atlas steels (not asserted), BUILT 2026-06-12** (`cct_validation.py` + demo + figure + 11 tests; fast lane **500→511 passed**; **ADR 0005**; no engine touch). Same cited US Steel 1951 IT atlas (IIIF native-res crops; 8620 *is* p.113), reads calibrated vs the 4340 anchor = **factor-2** (ranking-grade). Headline **bias-immune from 2 CITED anchors**: cited BC inverts 1080↔4340 by **×41 sign-inverted** (reproduces austemper's scale gap). Grading **(both metrics ANCHOR-INVARIANT — advisor's late catch: anchor-referenced "median miss" flips factor order on re-anchor; use Spearman + spread-std)**: **none combines rank+magnitude** — rank PC 0.81>FC 0.48>BC 0.10 (BC=wall, carbon-dominated); spread FC ×3.1 tightest, BC ×4.6 (flat/no-skill), PC ×6.7 widest (don't spotlight PC = cherry-pick) → **per-steel anchoring VINDICATED**; mechanistic = bainite retardation is **alloy-driven**, BC under-weights alloy. 1-knob refit (λ→0 carbon-deleted, holdout ρ0.4→0.8) = **diagnosis not law** (alloy-only ρ0.67≫Bs+grain 0.26; under-identified, 1080 lone no-alloy). 6 reads taken **hypothesis-aware** (advisor caught the confirmation-bias trap → rest headline on cited anchors). Extends [[bainite-anchoring-probe]] (2→8 steels); leaves §19 measured-CCT gap untouched (atlas isothermal); durable = [[di-crosscheck-source]] applied to my own visual reads
- [Commit/push at end of batch](commit-push-end-of-batch.md) — standing instruction (feedback): always commit AND push at end of a work batch; this repo is linear on `main` (no PR workflow) → commit + push there, don't branch
- [D_I cross-check source](di-crosscheck-source.md) — Steel Phase 6c BUILT 2026-06-10 (**Phase 6 COMPLETE**): critical-diameter / measured-Jominy cross-check (`ideal_diameter.py`, +15 tests, gate→354); benchmark MEASURED not Grossmann (else tautology — model rides Grossmann relative potencies) → **EMJ p.29 water-quench J→D_c conversion (applied identically both sides so its error CANCELS; an AI-extracted "Table A7 ideal-D_I" was DROPPED — it coincided with EMJ oil, below water, impossible for D_I; physics check D_I≥D_water caught it)** + SAE J1268/EMJ H-bands + SAE-J406/Hodge-Orehoski 50%M hardness (model J50 from fM=0.5, measured from cited h50 so model doesn't grade its own benchmark); teeth (read the SHAPE) = ranking-correct (1045 35<8620 51<4140 104<4340 119mm water-equiv, alloy beats carbon) + 4340 UNDER-predicted (Ni potency, Cr-Mo-calibrated scale) + shallow-grades-ride-high (knee); 4140=anchor-by-construction NOT teeth; 4340 benchmark-local; atlas-E-Q candidate dropped (IT-atlas p.105 collision); durable lesson = verify AI-extracted tables vs an independent direct read before baking absolute numbers; amends [[bainite-anchoring-probe]]/[[ferrite-bay-source]]
- [Ferrite bay source](ferrite-bay-source.md) — Steel Phase 6a: cited Li(1998)/Kirkaldy–Venugopalan proeutectoid-ferrite CCT kinetics + FC comp-factors (carbon 6.31 strong retarder; FC ratio 4140/1045≈32× = the teeth) that `kinetics.py` §5 pins; the durable diagnosis = "A₁-not-A₃" was a MISdiagnosis (A₁ right for pearlite), the deep 1045 knee = the unmodeled proeutectoid-ferrite reaction (additive bay, one knob `FERRITE_KINETIC_SCALE=8.0`); **6b DONE-as-descoped negative result** (cited BainiteReaction built standalone, BC Cr/Mo ≪ FC Cr/Mo ~29× gap = the bay's mechanism, but wiring it into pathint PROVEN to regress — 8620 carbon-spread ceiling; pathint byte-identical; forward fork: unified-KV rebuild vs 6c); 6c pending
- [Grain-growth source](grain-growth-source.md) — Steel Phase 5a: cited S960MC austenite grain-growth Arrhenius (Q=329.95 kJ/mol CITED + isothermal grain-size table) that grain.py pins; m/K0/D0 calibrated to the table (paper headline m=3.03 vs isothermal m≈4.2 NAMED); the holdout (fit 900&1200°C→predict 1000&1100°C within 16%) = Phase-5's only real teeth; ASTM E112 G↔d exact
- [Hollomon–Jaffe tempering source](hollomon-jaffe-tempering-source.md) — HJ tempering parameter + the C_hj≈20 constant Phase 3b uses
- [Martemper (§17)](martemper.md) — martempering = austempering's **short-hold sibling**, BUILT ✓ 2026-06-11 (suite 425→**447 green**, no new physics/constant, no engine touch). Same `Mₛ<T_bath<Bs` window, read for martensite; built on 6d `austemper.py`'s **anchored bainite kinetics** (advisor redirect — NOT re-derived from primitives). `critical_hold_time` = bath bainite-onset = the martemper↔austemper boundary (unifies equivalence + nose-avoidance); guard-discrimination check run BEFORE writing (single-pearlite-curve toothless near Mₛ → MUST use anchored kinetics). Equivalence = `ideal_quench` exact-by-construction (4340≡water exactly; 1080 marginally harder = cleaner nose-miss). **Distortion payoff** = surface−centre ΔT at Mₛ on the **frozen heat engine** (two-stage Robin slab, no Strang; hold-equalise + **slow-cool** both essential) = **62× reduction** (1080 20mm, resolution-converged), at the **direct-quench hardness point-for-point** (NOT full through-harden); thermal proxy, **NO solid mechanics** (residual stress = deferred §11 Option-#2). Feasibility `τ_eq<t_crit` (conservative hold-side proxy) → 4340 40mm plate FAILS (illustrates textbook limit; verdicts survive complete-dwell check). Per-steel only; t_crit-near-Mₛ optimistic. Amends [[bainite-anchoring-probe]]; §14/§16 "no new physics" sibling [[di-crosscheck-source]]/[[mixed-temper-next]]
- [Residual stress (§18)](residual-stress.md) — **§11 Option-#2 residual stress & distortion on quench BUILT ✓ 2026-06-12** (`residual.py` + demo + figure + 18 tests; suite **465 green**); the **first solid mechanics**, makes §17 [[martemper]] quantitative-in-stress. Crux (advisor): **incremental elastic–perfectly-plastic with T-dependent yield, NOT pure-elastic** (pure-elastic → 0 residual on a through-hardened part → inverts the risk ranking; residual is path-dependent, locked by hot-soft plastic yield). Equibiaxial 1-D plate on the **§17 slab** (advisor steer — NOT the Jominy fin), `σ=E(T)/(1−ν)·(ε*−ε_free−ε_pl)` clipped to `±σ_Y(T)`, bisect ε* for ∫σ=0; `ε_free`=thermal+KM martensite **dilatation** (cited lattice). **Teeth (both signs verified BEFORE tests):** sign reversal via **transform ON/OFF toggle** (4340 50mm water: OFF −141 compression → ON +386 tension; both atlas steels through-harden so thermal-only is the reference); **severity gate** (Biot≳1 needed to yield the hot core — mild quench leaves nothing); ∫σ=0 machine-precision; peak~yield; **martemper≪direct** (idealised near-removal). Cited Eurocode-3 E(T)/σ_Y(T)+lattice dilatation, representative ν/E₂₀/σ_Y,20/α — **no fitted number** (teeth structural). #1 edge = **no TRIP**; through-hardening-only; no engine touch/ADR. Plan §18 = as-built
- [MatCalc mc_fe database source](matcalc-mc-fe-database-source.md) — ODbL-licensed MatCalc steel TDB + bundled cfe_broshe Fe-C DB for Phase 4 CALPHAD (never committed)
- [Maynier hardness source](maynier-hardness-source.md) — Maynier 1978 constituent-hardness eqns/coeffs that Phase 3a grafted
- [Mixed-temper (§16)](mixed-temper-next.md) — mixed-structure (per-constituent) tempering, the named 3b deferral. **ALL STEPS 1–6 BUILT ✓ 2026-06-11** (no engine touch, no new constant; frozen 2c/3a/3b/Jominy/four-curves+`tempered_martensite_HV`/`hardness_HV` byte-identical; suite **425 green**). Steps 1–3 = the validated core (`properties.py` §6 `tempered_hardness_HV` rule-of-mixtures + `tempered_jominy_hardness` teeth + 3 exact seams; figure in HV not HRC; NO extracted numbers per [[di-crosscheck-source]]). **Steps 4–6 = the `design.py` guarded unlock**: `_is_temperable` (martensite-**dominant** 0.95→0.50 **AND** RA-**capped** 0.05, the load-bearing guard — data-grounded: hazard 1080-water RA 0.175 vs admit ≤0.035) inverts `tempered_hardness_HV` over the mixture (Vr threads, nan→None). **§14 headline re-derived (the crux, advisor Option B):** 1045-water now feasible (88% M) but **Biot-stretched** → **`recommended` made Biot-aware** (cheapest *lumped-valid*, fallback cheapest) → **textbook 4140-oil-temper HOLDS** (by design not accident; §14 *already* said "cheapest lumped-valid"). Rejected pure-cost-flip = reductio (a regression test asserting the #1 rec is flagged-invalid). `Recipe.martensite` + label cue; notebook §7 surgically refreshed (fixed a real `recipes[1:]` bug, 38 cells byte-identical). bainite/RA-inert still named-not-validated
- [Notebook kernel wedge root-cause](notebook-kernel-wedge-rootcause.md) — `test_steel_notebook` wedge ROOT-CAUSED 2026-06-10 = upstream pyzmq/asyncio-on-Windows lost-`execute_reply` (kernel idle 0% CPU, client never gets reply; load-INDEPENDENT ~24% in bare repetition, content-innocent cell-index-wanders, version-independent 3.13&3.14); **DON'T force `WindowsSelectorEventLoopPolicy`** (worse — timeout-length stall every run, can blow the 180s outer guard); mitigation = **retry-on-wedge** in the test (child exit 2 `CellTimeoutError`=retry vs 1 `CellExecutionError`=fatal; `MAX_ATTEMPTS=5`, verified 25/25 retries-recovered); **`_SKIP_IN_CI` stays** (separate unreproduced Ubuntu/Linux hang from a00f66a); amends [[steel-grain-physics-deferred]]
- [Notebook surgical insertion](notebook-surgical-insertion.md) — how to add a cell to `steel.ipynb` keeping every OTHER cell byte-identical: execute only `[setup, new_cell]` in a fresh kernel to embed output, **`copy.deepcopy` the setup cell first** (NotebookClient writes outputs back INTO the cells it runs → passing the real `nb.cells[3]` mutates it; the gitignored `outputs/probe_unified_kv/insert_nb.py` has this latent bug, only "got lucky" on a deterministic setup print), **hash-guard** every original before/after, write CRLF (`.replace("\n","\r\n")`), insert multi-section at ORIGINAL indices DESCENDING (no shift math). Static banked-figure cells (not `interact` — [[notebook-kernel-wedge-rootcause]]); markdown-only edits skip the kernel + the notebook test re-run. Used for §9 surfacing (5 sections, 2026-06-12) + §6b [[unified-kv-rebuild]]; sync [[gallery-page]] after
- [Repo must stay self-contained](repo-self-contained.md) — standing instruction (feedback, 2026-06-11): "don't count on monorepo future existence" → steel-sim is standalone, must not depend on the BigSim monorepo/archive; **directive-vs-record test** = fix live docs that send a reader to the monorepo, LEAVE dated plan/ADRs that merely record what the build was authored against; `download_mc_fe()` URL verified upstream (matcalc.at, not a BigSim mirror); applied = neutralized `ARCHITECTURE.md §N` cites in live surfaces + rewrote README Provenance
- [Pickering strength-DBTT source](pickering-strength-dbtt-source.md) — Steel Phase 5b: cited Pickering ferrite-pearlite yield + impact-transition-temperature (DBTT) equation pair (same Hall–Petch form, opposite grain-size signs) that grain.py §3 pins; source (2), independent of grain-growth-source; −11.5 grain & +44 Si web-confirmed, pearlite-in-yield the one calibrated coeff
- [Unified-KV rebuild (§19)](unified-kv-rebuild.md) — **the "6b deepening" BUILT ✓ 2026-06-12** (last `[available]` §11 item → menu empty; suite 465→**502 green**, no ADR/engine touch). `unified_kv.py` races **3 cited Li/KV reactions** (ferrite/pearlite/bainite) on one shared austenite pool + lever-rule carbon enrichment → the **bainite bay opens in CCT** (4340 intermediate→bainite ≈0.62; 1080 NO bay = consistency-with-austempering kicker). New `kinetics.py §7 PearliteReaction` (PC, ΔT³, Ae1, **√Mo**) — purely additive, frozen single-curve pipeline byte-identical. **Crux (advisor): per-steel-anchored DEMONSTRATOR not cross-steel predictor** — ferrite/pearlite differentials cited-and-RIGHT (PC ratio 1435×≈atlas ~10³× = teeth) but bainite BC cited-and-WRONG (0.146 = ~7× wrong-direction) → bainite per-steel atlas-anchored (→[[bainite-anchoring-probe]]/`austemper`), 8620 = the wall. Scale split: ferrite global 8.0 / pearlite global *derived-at-import* vs frozen 1080 nose / bainite per-steel. Edges: **no measured-CCT validation** (atlas isothermal, bridged by Scheil = demonstration — keep docs at "demonstrated"), carbon-enrichment first-order (feeds final Mₛ, kinetics bulk-comp), 1080/4340 only. Notebook §6b surgical insert (CRLF trap: nbformat writes LF, `.replace("\n","\r\n")`). Amends [[ferrite-bay-source]] (option (a) built); plan §19 = as-built
- [Steel grain-physics deferred](steel-grain-physics-deferred.md) — was a deferred schematic-cartoon stand-in; PROMOTED to Steel Phase 5, now COMPLETE (5a austenite grain-growth + ASTM-G / 5b cited Pickering yield+DBTT pair, same Hall–Petch form OPPOSITE grain-signs → refinement = lone strength-AND-toughness co-improver / 5c PAGS→ferrite coupling + co-benefit figure) & surfaced in steel.ipynb & app.py §5; **grain *morphology* (size-accurate Voronoi swatch) BUILT 2026-06-10** (viz-reach NOT physics, ADR 0002 — `plots.grain_voronoi_swatch`/`grain_morphology_figure` + `demo_grain_morphology` + app §5, ALONGSIDE not replacing the schematic; faithful quantity = grain number density `N_A=1/d²`, FIXED window so a slider coarsens the picture; gate 385→393; **notebook §5 cell ADDED** via a reusable *surgical single-cell insertion* — exec only `[setup+new cell]`, embed just its output, other 37 cells byte-identical (enabler: no-op nbformat round-trip is byte-identical); the `slow` `test_steel_notebook` wedge is now ROOT-CAUSED + retry-mitigated → [[notebook-kernel-wedge-rootcause]], NOT the "infra flake under full-load" it was once called); teeth = 5a's grain-growth holdout (sign-opposition is by-construction = demo NOT teeth)
