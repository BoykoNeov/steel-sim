---
name: pr-1469-root-cause
description: "Root-cause of ipython/ipykernel#1469 (enable ProactorEventLoop on Windows) breaking the debugger — ipykernel read-side, not debugpy"
metadata: 
  node_type: memory
  type: project
  originSessionId: f351fdc4-cc0c-4f67-943c-60f366e94d8e
---

ipython/ipykernel **#1469** ("Enable ProactorEventLoop on Windows", by NewUserHa; sibling of the team's own [[upstream-pr-filed]] #1529) — root-caused locally 2026-06-18 in worktree `M:\claud_projects\ipykernel-1469` (branch `pr-1469`, head `4032796`). `test_debugger.py::test_stop_on_breakpoint` is the failing test.

**Proven (primary evidence, debugpy's own logs via `DEBUGPY_LOG_DIR`):** the bug is **ipykernel's read side, NOT debugpy**. On the failing run debugpy hits the breakpoint, suspends MainThread, and the adapter writes `Client[1] <-- {event:"stopped"}` @649ms — but ipykernel's **idle control-thread `ProactorEventLoop` never wakes to read it** off the (still-open) zmq socket; it waits the full 100s. Earlier debug frames (seq 1–17: attach/setBreakpoints/threads…) were all read fine because the loop was actively `await`-driven then; `stopped` is the first frame arriving while the control loop is idle.

**2×2 (same instrumented build, only the named variable changes):** Proactor+3.13 → FAIL; Proactor+3.11 → PASS; forced-Selector+3.13 → PASS. ⇒ failure = **intersection Proactor × Python ≥3.12**, matching CI (Win 3.10/3.11 pass, 3.12/3.13/3.14/pypy fail).

**MECHANISM — PROVEN 2026-06-18 (faulthandler + 3.11 mirror):** asyncio `ProactorEventLoop` has **no** native `add_reader` (both versions `NotImplementedError`) → tornado wraps it with `AddThreadSelectorEventLoop`, whose **"Tornado selector"** helper thread does `select()` then `call_soon_threadsafe` (asyncio.py `_run_select:625`) to wake the loop. ipykernel exempts its Control/IOPub threads from debugpy but NOT tornado's helper. **3.13 faulthandler stack of the wedged kernel = the helper frozen at `_run_select:625` inside `call_soon_threadsafe` → `do_wait_suspend`** (select already returned; frozen before it can wake the loop; the Proactor loop sits in IOCP `_poll` forever = deadlock). **3.11 = helper completes call_soon_threadsafe, `Control HANDLE_SELECT` runs** → stopped read → pass. **Version delta = pydevd backend:** 3.11 `sys.settrace` (per-thread) doesn't freeze the tornado-owned helper; 3.13 `sys.monitoring` (PEP669, interpreter-global; compiled `_pydevd_sys_monitoring_cython` loaded) instruments all threads incl. the helper. (`Marking thread suspended: Tornado selector` appears on BOTH — wrong test; "frozen AT call_soon_threadsafe" only on 3.13 — right test.)

**FIX A = BUILT & TESTED 2026-06-18 (the ship).** Run ALL ipykernel *service* loops on a `SelectorEventLoop` (native add_reader, no "Tornado selector" helper) and keep ONLY the main/user-code loop on Proactor (so #1468 `create_subprocess_exec` works). = pre-#1469 loop type for every loop except main. **2 tracked files, ~20 lines, win32-guarded, NO pydevd coupling:** `thread.py` new `make_selector_io_loop()` (`IOLoop(make_current=False, asyncio_loop=asyncio.SelectorEventLoop())`) used by `BaseThread` (Control/Shell-channel/**subshells** — `SubshellThread(BaseThread)`); `iostream.py` `IOPubThread` uses it (IOPub MUST be Selector — control republishes `stopped` onto IOPub's zmq event-pipe `add_reader`, same freezable helper otherwise). Main loop (`IOLoop.current()`) untouched. Mergeable: `asyncio_loop=` kwarg is tornado≥6.2, floor is `tornado>=6.4.1`. **Verified 3.13 .venv Proactor (spike+instrumentation reverted first):** full `test_debugger.py` = **1 failed/10 passed/14.95s** (all 5 breakpoint tests pass; lone fail `test_attach_debug 'ab'==''` IDENTICAL to baseline+spike = pre-existing) — matches spike's 1f/10p/14.5s. #1468 half confirmed by probe (main=ProactorEventLoop, Control+IOPub=_WindowsSelectorEventLoop no-helper). Regression io/subshells/start_kernel: NO new failures vs baseline — the `WinError 10038` IOPub-teardown flake + `Task was destroyed` warnings are **pre-existing, loop-type-independent** (both architectures use `selectors.SelectSelector`). **Cross-version: `test_debugger.py` @ .venv311 (3.11) = SAME 1f/10p** (3.11 never broke → Fix A harmless on it). **CORRECTION 2026-06-18 (earlier "NOT loop-related" was WRONG): `test_attach_debug` IS a 2nd, distinct #1469 regression — BISECTED same box/venv/debugpy-1.8.21: `origin/main` Selector = PASS (`''`) vs `pr-1469` Proactor = FAIL (`'ab'`), and pure-#1469 (Fix A stashed) FAILs too ⇒ caused by Proactor, not Fix A. BUT cosmetic, NOT functional: stateful probe under BOTH loops (`a=5`→`''`; `a`→`'5'`; `a+100`→`'105'`) proves the repl fully works under Proactor (namespace persists, exprs compute/surface). Only the FIRST repl-`evaluate`'s DAP `result` field differs — empty under Selector, actual value under Proactor (Proactor surfaces it one evaluate earlier; arguably MORE correct). Fix A does NOT address it (evaluate runs on the MAIN loop, which stays Proactor for #1468 → out of Fix A's scope). Editing `==''` would BREAK CI under Selector ⇒ it's a NOTE for #1469's author, not a stale-test PR.** **`test_kernel.py` @3.13 = 24p/6skip** (control+IOPub round-trip = primary blast radius, green). **PR note: subshells now Selector → `create_subprocess_exec` works in main shell NOT inside a subshell on Windows (intentional, =pre-#1469).** **Fix A applied UNCOMMITTED in worktree; nothing posted to PR.**

**Fix direction (grounded):** PROVEN all-versions = keep **control/debug loop on a Selector loop** (Control thread is suspension-exempt → no helper thread in the read path); targeted PR could let shell/main be Proactor (subprocess support) while control stays Selector. **`AddThreadSelectorEventLoop` = RULED OUT as-is** (its helper thread IS the frozen "Tornado selector"). **Helper-exemption fix (Fix B) = SPIKE DONE, PROVEN 2026-06-18 (causal proof, NOT the ship):** set `is_pydev_daemon_thread`+`pydev_do_not_trace` on tornado's "Tornado selector" thread BEFORE `_thread.start()` (so ThreadInfo built `trace=False`; pydevd sys.monitoring honors both — `_pydevd_sys_monitoring.py:379`/`:1889`). Full `test_debugger.py` 3.13 Proactor A/B: baseline = **5 breakpoint tests deadlock** (6 failed/5 passed/**1014s**); with flag = **all 5 pass** (1 failed/10 passed/**14.5s**). Lone remaining fail `test_attach_debug` (`'ab'=='' `) is IDENTICAL both sides = NOT Fix A — it's #1469's Proactor change (see CORRECTION above: loop-correlated but cosmetic). **Spike vehicle = venv tornado edit; NOT the fix home** (tornado won't take pydev attrs → upstream-viable = ipykernel locate-and-flag [fragile] OR pydevd auto-exempt; each needs its own PR).

**2026-06-19: NewUserHa replied to the Fix A comment with 3 points; rebutted with primary-source evidence (issuecomment-4749271251).** (1) "`IOLoop` has no `asyncio_loop` param, passed anonymously into `AsyncIOLoop`" — half-right (it flows via `**kwargs`, base sig doesn't name it) but the CONCLUSION is wrong: `AsyncIOLoop.initialize` handles it EXPLICITLY (`if "asyncio_loop" not in kwargs:` branch, tornado/platform/asyncio.py:328-332), DOCUMENTED public API since **tornado 6.2** (`.. versionchanged:: 6.2`), our floor `6.4.1` safely above; direct `AsyncIOLoop` use is **deprecated since 5.0** → `IOLoop(asyncio_loop=...)` is the RECOMMENDED path. **KEEP current approach, do NOT switch to explicit construction.** (2) "reuse `BaseAsyncIOLoop`?" — we already are; lines 104-110 show `ProactorEventLoop → AddThreadSelectorEventLoop` (= the freezing helper); a `SelectorEventLoop` makes `selector_loop is asyncio_loop` so the wrapper is never built. (3) "still 1 failed" = the cosmetic `test_attach_debug` (single assertion `test_debugger.py:122` `== ""`); offered to push the Proactor value to `pr-1469`. **Advisor catch before posting: my point-2 code block was reconstructed from `grep` fragments (dropped `self.selector_loop = asyncio_loop` + the `hasattr` guard) → re-Read verbatim before quoting; also softened point-1 tone to grant the `**kwargs` observation first.** Verified against installed tornado 6.5.7 in `.venv`.

2026-06-18: Fix A committed `b142cae` on `pr-1469` (verified = #1469 PR head + exactly this 1 commit) and pushed to fork `BoykoNeov/ipykernel`. **Root-cause+fix COMMENT POSTED on #1469** (issuecomment-4741816869): explains the helper-thread freeze, links the branch, offers a PR into `patch-2`, and flags the cosmetic `test_attach_debug` change as a separate low-severity item. Per user choice, **no standalone PR opened** (current `main` is Selector-everywhere → a PR-to-main reads as a near-no-op; the #1469 comment is load-bearing — NewUserHa had explicitly asked for exactly this help). PR body draft = `PR_DRAFT.md`, comment = `COMMENT_1469_DRAFT.md` in the worktree. Earlier: spike block (venv tornado) + tracked-file instrumentation (`debugger.py`/`ipkernel.py`/`kernelbase.py`) REVERTED before the Fix A test run. Untracked helpers (`_abtrace.py`, `_probe/`, logs) now dead, safe to delete. Full writeup = `FINDINGS_1469.md` in the worktree.
